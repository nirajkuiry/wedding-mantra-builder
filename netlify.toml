[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Client-side routing (React Router) — without this, refreshing on
# /packages, /builder, /settings, or /admin gives a 404 on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
